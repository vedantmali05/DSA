================================================================================
ARRAYS CONCEPTUAL ASSESSMENT
================================================================================

Range of topics:
* Basic Array Manipulations (01_01 to 01_09)
* Searching Algorithms (02_01 to 02_05)
* Basic Sorting (03_01 to 03_02)

--------------------------------------------------------------------------------
QUESTIONS
--------------------------------------------------------------------------------

🟢 Q1. Why does the standard middle index formula `(left + right) / 2` cause an overflow in Java when working with large arrays, and how does `left + (right - left) / 2` algebraically resolve this without exceeding the array boundary values?
Ans: 

🟢 Q2. When searching in a rotated sorted array, explain why the presence of duplicate elements (e.g., searching for `0` in `{2, 2, 2, 2, 0, 2}`) degrades the time complexity from O(log N) to O(N). What comparison fails to guide the binary search decision?
Ans: 

🟢 Q3. Is Selection Sort stable or unstable? Provide a brief step-by-step trace of a small example array with duplicate elements (e.g., `{4, 4, 1}`) that demonstrates how its relative duplicate order is violated.
Ans: 

🟢 Q4. Explain the three-reversal algorithm to rotate an array of size N to the right by K places. Why is it preferred over auxiliary array approaches in production?
Ans: 

🟢 Q5. In the two-pointer technique for removing duplicates from a sorted array in-place, why does the write pointer only write when `arr[read] != arr[write - 1]`? What logical bug occurs if we check `arr[read] != arr[read - 1]` instead?
Ans: 

🟢 Q6. When moving all zeroes to the end of an array while maintaining the relative order of non-zero elements, why does a partitioning approach (like QuickSort's swap-based partition) fail to preserve order, and how does the two-pointer compaction method solve this?
Ans: 

🟢 Q7. When finding the single missing number in an array containing elements from `1` to `N`, what is the technical risk of using the mathematical sum formula `N * (N + 1) / 2` in Java, and how does the Bitwise XOR method bypass this?
Ans: 

🟢 Q8. When reversing an array recursively, what is the auxiliary space complexity in terms of call stack frames? Compare this with the space complexity of the iterative two-pointer array reversal.
Ans: 

🟢 Q9. In binary search for the first occurrence of a duplicate target element, why do we continue updating the search space boundaries (specifically setting `right = mid - 1`) even after finding `arr[mid] == target`?
Ans: 

🟢 Q10. In the optimized Bubble Sort, why is the best-case time complexity O(N), and what condition triggers the early exit? Explain why the unoptimized version fails to achieve this.
Ans: 

🟢 Q11. If you are given an unsorted array of size N and need to perform a single search query, is it better to sort the array first and perform a Binary Search, or simply perform a Linear Search? Explain your answer using time complexities.
Ans: 

🟢 Q12. If you are writing software for an embedded system with flash memory where memory write operations (swaps) degrade the hardware lifespan, which sorting algorithm is better: Bubble Sort or Selection Sort? Why?
Ans: 
